@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
<center><h2>Users Management</h2></center>
<a asp-controller="App" asp-action="Home"> &lt; Go Back</a>
<br />
<br />
<button class="btn btn-dark"><a asp-controller="Account" asp-action="NewRegistration" class="nav-link" style="color:aqua">Add User</a></button>
<br />
<br />
<div>
    <center>
        <form asp-action="UserManagement" method="get">
            <div class="form-actions no-color">
                <p>
                    Search by <b>UserName</b> or <b>Email id</b>: <input type="text" name="SearchString" value="@ViewData["CurrentFilter"]" />
                    <input type="submit" value="Search" class="btn btn-info" /> |
                    <a asp-action="UserManagement">Back to Full List</a>
                </p>
            </div>
        </form>
        <table class="table table-striped table-bordered" style="margin-left:30px;margin-right:30px; margin-left:30px">
            <thead class="thead-dark">
                <tr>
                    <th> UserName </th>
                    <th> Email address </th>
                    <th> Not Enabled? </th>
                    <th style="color:darkcyan"> Update </th>
                    <th style="color:darkcyan"> Delete </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var p in Model)
                {
                <tr>
                    <td>  @p.UserName </td>
                    <td>  @p.Email </td>
                    <td>  @p.LockoutEnabled </td>
                    <td>
                        @Html.ActionLink("Enable", "Enable", new { id = p.Id })
                    </td>
                    <td>
                        @Html.ActionLink("Delete", "Delete", new { id = p.Id })
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </center>
</div>